<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, label: "Your review" %>
  <%= f.input :rating, collection: 0..5, as: :select, label: "Rating" %>
  <%= f.submit "Submit Review", class: "btn btn-success" %>
<% end %>
